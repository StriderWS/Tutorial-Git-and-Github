git config --global user.name "Seu nome de usuário"
git config --global user.email "Seu endereço de e-mail"
git config --global core.editor "Nome do seu editor de texto"

git config --list

==========================

git init

git status = ira mostrar: (Commits), (Arquivos ainda não adicionado)
git add "Nome do Arquivo que aparece em vermelho no gitbash" 

git add "Teste.txt" = O arquivo está pronto para ser adicionado ao controle de versão. Caso você digite novamente o git status, ele ira mostrar o arquivo na cor verde agora

(Caso você queira adicionar varios arquivos é só usar o comando "git add .")

git commit -m "Mensagem sobre a versão adicionada"

git remote add origin "Link do repositorio" = você definindo para qual repositorio os arquivos tem que ser enviados

git push = Para Enviar
===========================

Comados Usados no total

git init
git status
git add "Aprendendo - PHP"
git commit -m "My PHP learning projects #1"
git remote add origin "https://github.com/StriderWS/FatecProjects.git"
git push

